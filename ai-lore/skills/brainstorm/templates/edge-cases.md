# Edge Cases: {{title}}

<!-- Soft budget: 50 lines. Cover: empty states, null/missing data, concurrent access, over-limit inputs, graceful degradation. -->
<!-- Decision tree: include a mermaid `flowchart TD` only if there are 4 or more branching outcomes (max 12 nodes); otherwise the table reads faster. -->

| Edge case | Expected behavior | Accounted for? |
|---|---|---|
| {{describe the case}} | {{what the system should do, user-facing terms}} | {{yes / no / partially, one line}} |

{{Optional short prose for any case whose expected behavior needs more than a table cell. Keep it to the cases that genuinely need it.}}
