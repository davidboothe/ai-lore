---
from: 0.5.0
to: 0.6.0
config_changes: false
---

No config changes. Added `ail-review` skill and `ai-lore:code-reviewer` agent. The `runs.yaml` registry gains two optional fields (`review_status`, `review_file`) written by `ail-review`; no migration needed for existing runs. Update `plugin_version` to `0.6.0`.
