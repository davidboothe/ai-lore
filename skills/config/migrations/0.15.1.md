---
from: 0.15.0
to: 0.15.1
config_changes: false
---

No config changes. `ail-review` now lists every finding (blocking and advisory) in its inline session summary rather than echoing only blocking findings; the full detail still lives in `review.md`. Update `plugin_version` to `0.15.1`.

## One-time migration steps

1. Update `plugin_version: 0.15.1` in config.yaml.
