---
from: 0.7.6
to: 0.8.0
config_changes: false
---

No config changes. Workflow scripts moved from external `workflows/*.js` files to inline `script` parameters in each SKILL.md. The `workflows/` directory has been removed. Update `plugin_version` to `0.8.0`.
