---
from: 0.14.0
to: 0.15.0
config_changes: false
---

No config changes. `ail-document` now reviews poor-fit concept assignments: a new directory that lands in an existing concept only by a broad `owns_paths` glob (and whose `candidate_concepts` diverge from that concept) is surfaced to the user, who may split it into a new concept. Update `plugin_version` to `0.15.0`.

## One-time migration steps

1. Update `plugin_version: 0.15.0` in config.yaml.
