---
from: 0.6.1
to: 0.6.2
config_changes: false
---

No config changes. Workflow scripts extracted from SKILL.md prose into versioned `.js` files under `workflows/` (`state-check.js`, `build-wave.js`, `review-dimensions.js`). Skills now invoke them via `Workflow({ scriptPath })` instead of inline script strings. Update `plugin_version` to `0.6.2`.
