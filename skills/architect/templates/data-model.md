# Data Model

<!-- Generated only when the feature involves entities, schema, relationships, or persistent state. Soft budget: 80 lines. -->
<!-- One home per fact: entities and their shapes live here. overview.md points here; api.md references entities by name without restating their fields. -->

## Entities

<!-- One subsection per entity: fields, types, constraints, indexes. A mermaid erDiagram is conditional: use only when 3+ entities relate to each other; max 8 entities per diagram, split with captions if larger. -->

### {{Entity}}

{{fields, types, constraints, indexes; a table or a tight list}}

## Relationships

{{how entities relate; cardinality and ownership}}

## Schema Notes

{{archival strategy, naming conventions, soft-delete approach if used. If rollout.md exists, migration steps live there; this section holds only schema-shape notes.}}
